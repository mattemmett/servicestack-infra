# Remote State Bootstrap

Use this stack to create and manage the remote state backend before applying the main environment stacks.

Suggested resources:
- S3 bucket for state
- optional locking table
- versioning and encryption defaults
